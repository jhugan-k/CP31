#include<bits/stdc++.h>
using namespace std;

/*
> can always place at corner 1,1 and n,n where n is larger number in a x b
*/
int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int tc;
    cin >> tc;
    for(int t = 0; t < tc; t++){
        int a, b;
        cin >> a >> b;
        int x, y;
        cin >> x >> y;
        cout << 1 << " " << 1 << " ";
        cout << a << " " <<  b << endl;
        
    }
}